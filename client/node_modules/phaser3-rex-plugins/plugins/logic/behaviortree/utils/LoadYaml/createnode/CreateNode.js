import CreateSelector from './CreateSelector.js';

const Handlers = {
    'selector': CreateSelector,
}

var CreateNode = function (data) {
    return Handlers[data.__category__](data, CreateNode);
}

export default CreateNode;