import { Selector } from '../../../nodes/';

const ReservedKeys = ['children'];

var CreateSelector = function (data) {
    var childrenData = data.children;
    if (childrenData) {

    }

    var selector = new Selector({
        // children: []
    })

    // Find decorators
    var decorators = [];
    for (var key in data) {
        if (ReservedKeys.indexOf(key) !== -1) {
            continue;
        }
        decorators.push(data[key]);
    }

    if (decorators.length === 0) {
        return selector;
    } else {

    }

}
export default CreateSelector;